{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug active file (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C/C++: clang++ build active file",
            "terminal": "integrated"  // or "external" if you want a macOS Terminal window
        }
    ]
}
